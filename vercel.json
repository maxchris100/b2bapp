{
    "version": 2,
    "builds": [
        {
            "src": "server/index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/client/$1"
        },
        {
            "src": "/api/trpc/(.*)",
            "dest": "/server/index.ts"
        }
    ]
}