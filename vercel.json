{
    "version": 2,
    "builds": [
        {
            "src": "index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/client/$1"
        },
        {
            "src": "/api/trpc/(.*)",
            "dest": "/index.ts"
        }
    ]
}